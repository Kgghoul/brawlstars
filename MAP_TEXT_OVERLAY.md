# ✅ Текст поверх карты в области затемнения

**Дата:** 2026-02-07  
**Задача:** Отображать текст (название и винрейт) поверх карты в затемнённой области, как в оригинальном дизайне

---

## 🎯 Проблема

После добавления центрирования одиночной карты, текст отображался **под картой**, а не **поверх неё** в области с градиентным затемнением. Это не соответствовало оригинальному дизайну, где текст накладывался на изображение.

---

## ✅ Решение

### 1. **HTML структура изменена**

Текст теперь находится **внутри контейнера карты**, а не снаружи:

#### `src/app/analytics/analytics.html`

```html
<!-- ДО: -->
<div class="map-centered">
  <img [src]="map.image" [alt]="map.name" />
</div>
<div class="text-wrapper-centered-wr">{{ map.winRate }}%</div>
<div class="text-wrapper-centered-name">{{ map.name }}</div>

<!-- ПОСЛЕ: -->
<div class="map-centered">
  <img [src]="map.image" [alt]="map.name" />
  @if (bestMaps.length === 1) {
  <div class="text-wrapper-centered-wr">{{ map.winRate }}%</div>
  <div class="text-wrapper-centered-name">{{ map.name }}</div>
  }
</div>
```

**Ключевое изменение:** Текстовые элементы перемещены **внутрь** `.map-centered`

---

### 2. **CSS позиционирование**

Текст теперь **абсолютно позиционирован** относительно контейнера карты:

#### `src/app/analytics/analytics.scss`

```scss
/* ДО - текст был относительным (outside) */
.screen-content .text-wrapper-centered-wr {
  position: relative;
  margin-top: 8px;
  text-align: center;
}

/* ПОСЛЕ - текст абсолютный (overlay) */
.screen-content .text-wrapper-centered-wr {
  position: absolute;        // Абсолютное позиционирование
  bottom: 25px;              // От нижнего края
  left: 50%;                 // От левого края
  transform: translateX(-50%); // Центрирование
  z-index: 2;                // Поверх затемнения
  color: #00ff26;
  font-size: 20px;
  font-weight: 600;
}

.screen-content .text-wrapper-centered-name {
  position: absolute;
  bottom: 8px;
  left: 50%;
  transform: translateX(-50%);
  z-index: 2;
  color: #ffffff;
  font-size: 10px;
}
```

**Ключевые свойства:**
- `position: absolute` - позиция относительно `.map-centered`
- `bottom: 25px` / `bottom: 8px` - отступ снизу
- `left: 50%; transform: translateX(-50%)` - центрирование по горизонтали
- `z-index: 2` - поверх градиента (градиент имеет `z-index: 1` по умолчанию)

---

### 3. **Условная логика**

Текст отображается поверх **только для одиночных карт**, для множественных карт используется старая логика:

```html
@if (bestMaps.length === 1) {
  <!-- Текст внутри карты (overlay) -->
  <div class="text-wrapper-centered-wr">{{ map.winRate }}%</div>
  <div class="text-wrapper-centered-name">{{ map.name }}</div>
}
@if (bestMaps.length > 1) {
  <!-- Текст снаружи карты (standard) -->
  <div [class]="'text-wrapper-' + (i === 0 ? '5' : ...)">
    {{ map.winRate }}%
  </div>
  <div [class]="'text-wrapper-' + (i === 0 ? '4' : ...)">
    {{ map.name }}
  </div>
}
```

---

## 🎨 Визуальный результат

### До изменений:
```
┌─────────────────────┐
│                     │
│     [изображение]   │
│                     │
└─────────────────────┘
        Hard Rock Mine  ← Текст под картой
            0%
```

### После изменений:
```
┌─────────────────────┐
│                     │
│     [изображение]   │
│     ▓▓▓▓▓▓▓▓▓▓▓     │  ← Градиентное затемнение
│     Hard Rock Mine  │  ← Текст ПОВЕРХ затемнения
│          0%         │
└─────────────────────┘
```

---

## 📋 Где применено

1. ✅ **analytics.html**:
   - "ЛУЧШИЕ КАРТЫ" - винрейт зелёный (`#00ff26`)
   - "ХУДШИЕ КАРТЫ" - винрейт красный (`#ff4040`)

2. ✅ **analytics1.html**:
   - "АНАЛИЗ КАРТ" - цвет винрейта зависит от значения (`> 50` = зелёный, иначе красный)

---

## 🔍 Слои (z-index)

```
┌─────────────────────────────────┐
│  3. Текст (z-index: 2)          │  ← Сверху
│  2. Градиент (::after)          │
│  1. Изображение (<img>)         │  ← Снизу
└─────────────────────────────────┘
```

---

## 🚀 Деплой

Изменения задеплоены на:
- **GitHub:** https://github.com/Kgghoul/brawlstars (commit: 4d1c052)
- **GitHub Pages:** https://kgghoul.github.io/brawlstars/

---

## 🧪 Тестирование

Проверьте локально:
```bash
ng serve
# Откройте http://localhost:4200
```

**Проверьте:**
1. Текст отображается **поверх** карты
2. Текст находится в **затемнённой области**
3. Текст **читаемый** (достаточный контраст)
4. Винрейт зелёный для лучших, красный для худших

---

**Результат:** Текст теперь красиво отображается поверх карты в области градиентного затемнения, как в оригинальном дизайне! 🎨✨
