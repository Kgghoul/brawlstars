# Quick Start Guide

## 🚀 5-минутный запуск

### Вариант 1: Используйте развернутое приложение (самое простое!)

1. Откройте веб-приложение: https://kgghoul.github.io/brawlstars/
2. Найдите бота в Telegram: [@primerbaotbot](https://t.me/primerbaotbot)
3. Отправьте `/start` и нажмите кнопку "Открыть аналитику"

**Готово! Всё работает!** 🎉

---

### Вариант 2: Локальная разработка

#### Angular приложение

```bash
# 1. Установите зависимости
npm install

# 2. Запустите dev-сервер
npm start

# 3. Откройте браузер
# http://localhost:4200
```

#### Telegram бот

```bash
# 1. Перейдите в папку бота
cd telegram-bot

# 2. Установите Python зависимости
pip install -r requirements.txt

# 3. Создайте файл .env
cp env.example .env

# 4. Отредактируйте .env и укажите BOT_TOKEN
# (Получите токен у @BotFather в Telegram)

# 5. Запустите бота
python bot.py
```

---

### Вариант 3: Docker (продвинутый)

```bash
# Запуск Angular приложения
docker build -t brawlstars-app .
docker run -p 80:80 brawlstars-app

# Или запуск всего через docker-compose
docker-compose up -d
```

---

## 📋 Требования

### Для локальной разработки Angular:
- Node.js 18+ (рекомендуется 20+)
- npm 9+

### Для Telegram бота:
- Python 3.11+
- pip

### Для Docker:
- Docker 20+
- Docker Compose 2+

---

## 🔑 Основные команды

### Angular

```bash
npm start              # Запуск dev-сервера
npm run build          # Production сборка
npm test               # Запуск тестов
ng generate component  # Создать новый компонент
```

### Telegram Bot

```bash
python bot.py          # Запуск бота
python api.py          # Запуск mock API
python run_all.py      # Запуск бота + API
python test_bot.py     # Тест подключения
```

### Git

```bash
git status             # Проверить статус
git add .              # Добавить все изменения
git commit -m "..."    # Создать коммит
git push origin main   # Отправить в GitHub
```

### Docker

```bash
docker build -t app .  # Собрать образ
docker run -p 80:80 app # Запустить контейнер
docker ps              # Список контейнеров
docker logs <id>       # Посмотреть логи
docker-compose up -d   # Запустить все сервисы
docker-compose down    # Остановить все сервисы
```

---

## 🌐 Важные ссылки

| Сервис | URL |
|--------|-----|
| **Веб-приложение** | https://kgghoul.github.io/brawlstars/ |
| **API Swagger** | http://91.229.11.191:8080/swagger/index.html |
| **Telegram Bot** | https://t.me/primerbaotbot |
| **GitHub Repo** | https://github.com/Kgghoul/brawlstars |

---

## 🆘 Troubleshooting

### "npm install" не работает
```bash
# Очистите кеш и попробуйте снова
npm cache clean --force
rm -rf node_modules package-lock.json
npm install
```

### "Port 4200 is already in use"
```bash
# Используйте другой порт
ng serve --port 4201
```

### Telegram бот не отвечает
1. Проверьте правильность BOT_TOKEN в `.env`
2. Убедитесь, что бот запущен: `python bot.py`
3. Проверьте логи в консоли

### API не доступен
```bash
# Проверьте доступность
curl http://91.229.11.191:8080/swagger/index.html

# Или откройте в браузере
```

---

## 📚 Дополнительная документация

- [README.md](README.md) - Полное описание проекта
- [API.md](API.md) - Документация API
- [DEPLOYMENT.md](DEPLOYMENT.md) - Руководство по развертыванию
- [PROJECT_SUMMARY.md](PROJECT_SUMMARY.md) - Итоговая сводка

---

## ✨ Полезные советы

1. **Разработка UI**: Используйте `ng serve` для hot-reload
2. **Тестирование API**: Используйте Swagger UI или Postman
3. **Отладка бота**: Смотрите логи в консоли при запуске
4. **Быстрый деплой**: `ng build && npx angular-cli-ghpages`

---

**Удачи! 🚀**
