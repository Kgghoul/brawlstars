# ✅ Центрирование одиночных карт

**Дата:** 2026-02-07  
**Задача:** Когда карта одна, отображать её по центру вместо размещения слева

---

## 🎯 Проблема

При наличии только одной карты (Hard Rock Mine для игрока 101), карта отображалась в левой части контейнера, оставляя много пустого пространства справа. Это выглядело несимметрично и некрасиво.

---

## ✅ Решение

Добавлена логика для центрирования карты, когда она одна:

### 1. **HTML изменения**

#### `src/app/analytics/analytics.html`
```html
<!-- ДО: -->
<div class="frame-4">
  @for (map of bestMaps; track map.name; let i = $index) {
  <div [class]="'map-container ' + (i === 0 ? 'mask-group' : ...)">
    <img [src]="map.image" [alt]="map.name" />
  </div>
  }
</div>

<!-- ПОСЛЕ: -->
<div class="frame-4" [class.single-map]="bestMaps.length === 1">
  @for (map of bestMaps; track map.name; let i = $index) {
  <div [class]="'map-container ' + (bestMaps.length === 1 ? 'map-centered' : ...)">
    <img [src]="map.image" [alt]="map.name" />
  </div>
  }
</div>
```

**Изменения:**
- Добавлен класс `single-map` когда `bestMaps.length === 1`
- Используется класс `map-centered` вместо позиционирования слева/по центру/справа
- Добавлены классы `text-wrapper-centered-wr` и `text-wrapper-centered-name` для винрейта и названия

#### `src/app/analytics1/analytics1.html`
Аналогичные изменения для секции "АНАЛИЗ КАРТ":
- Добавлена проверка `mapsAnalysis.length === 1`
- Скрыта иконка "i" когда карта одна: `@if (mapsAnalysis.length > 1)`

---

### 2. **CSS стили**

#### `src/app/analytics/analytics.scss` и `src/app/analytics1/analytics1.scss`

```scss
/* Центрирование одной карты */
.screen-content .frame-4.single-map {
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
}

.screen-content .map-centered {
  position: relative !important;
  top: auto !important;
  left: auto !important;
  width: 100px !important;
  height: 150px !important;
  overflow: hidden;
  border-radius: 8px;

  img {
    width: 100%;
    height: 100%;
    object-fit: cover;
    border-radius: 8px;
  }

  &::after {
    content: '';
    position: absolute;
    bottom: 0;
    left: 0;
    right: 0;
    height: 60%;
    background: linear-gradient(to bottom, transparent 0%, rgba(0, 0, 0, 0.9) 100%);
    pointer-events: none;
    border-radius: 0 0 8px 8px;
  }
}

.screen-content .text-wrapper-centered-wr {
  font-family: "TT Firs Neue-DemiBold", Helvetica, Arial, sans-serif;
  font-size: 18px;
  font-weight: 600;
  text-align: center;
  margin-top: 8px;
  position: relative;
}

.screen-content .text-wrapper-centered-name {
  color: #ffffff;
  font-family: "TT Firs Neue-Medium", Helvetica, Arial, sans-serif;
  font-size: 12px;
  text-align: center;
  margin-top: 4px;
  position: relative;
}
```

**Ключевые моменты:**
- `flexbox` для центрирования контейнера
- Относительное позиционирование вместо абсолютного
- Размер карты увеличен для лучшей видимости (100x150px)
- Текст выровнен по центру

---

## 📊 Где применено

1. ✅ **analytics.html** - "ЛУЧШИЕ КАРТЫ" и "ХУДШИЕ КАРТЫ"
2. ✅ **analytics1.html** - "АНАЛИЗ КАРТ"
3. ✅ **analytics3.html** - (уже отображает одну карту, изменения не требуются)

---

## 🎨 Визуальный результат

### До изменений:
```
┌─────────────────────────┐
│  ЛУЧШИЕ КАРТЫ          │
│                         │
│  [карта]   [ ]   [ ]    │  ← Карта слева, много пустого места
│  Hard Rock Mine         │
│  0%                     │
└─────────────────────────┘
```

### После изменений:
```
┌─────────────────────────┐
│  ЛУЧШИЕ КАРТЫ          │
│                         │
│       [карта]           │  ← Карта по центру!
│   Hard Rock Mine        │
│         0%              │
└─────────────────────────┘
```

---

## 🚀 Масштабируемость

Логика автоматически адаптируется:
- **1 карта:** Центрирование ✅
- **2-3 карты:** Стандартное расположение (left, center, right) ✅
- **Больше:** Легко расширить логику

---

## 📦 Деплой

Изменения задеплоены на:
- **GitHub:** https://github.com/Kgghoul/brawlstars (commit: 64d1cc1)
- **GitHub Pages:** https://kgghoul.github.io/brawlstars/

---

## 🔍 Тестирование

Проверьте локально:
```bash
ng serve
# Откройте http://localhost:4200
```

Страницы для проверки:
1. `/analytics` - Главная аналитика
2. `/analytics1` - Детальная аналитика

---

**Результат:** Теперь одиночная карта отображается красиво и симметрично по центру! 🎯
